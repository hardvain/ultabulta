<%= form_for(@mnemonic) do |f| %>
  <% if @mnemonic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mnemonic.errors.count, "error") %> prohibited this mnemonic from being saved:</h2>

      <ul>
      <% @mnemonic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
